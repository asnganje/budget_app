<section class="bg-gray-200 min-h-screen mt-0 px-8 lg:px-5">
  <%=render "shared/navbar"%>
  <div class="max-w-6xl mx-auto flex flex-col">
    <turbo-frame id="groups">
      <%= render "groups/groups", groups: @groups %>
    </turbo-frame>
    <%=link_to "Add a new category", new_group_path, data: { turbo_frame: "modal" }, class:"bg-blue-400 hover:bg-blue-300 py-2 px-5 w-1/2 lg:w-1/5 text-center text-gray-100 font-bold rounded-lg my-10" %>
  </div>
  <turbo-frame id="modal"></turbo-frame>
</section>